---
// se ejecuta en el servidor o en build time <----
const { title } = Astro.props;
---

<section class="max-w-[700px] mt-0 mx-auto mb-[48px]">
  {title && <h2 class="mb-2 font-bold text-[1.3pmrem] leading-6">{title}</h2>}
  <slot />
</section>
