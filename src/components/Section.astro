---
// se ejecuta en el servidor o en build time <----
const { title } = Astro.props;
---

<section class="max-w-[700px] mt-0 mx-auto mb-[48px]">
  {title && <h2 class="mb-2 font-bold text-2x1 leading-6">{title}</h2>}
  <slot />
</section>

<!-- <script>
  // se ejecuta en el cliente <----
</script>

<style>
  section {
    max-width: 700px;
    margin: 0 auto 48px;
  }

  h2 {
    margin-bottom: 8px;
    font-weight: 700;
    line-height: 1.5;
    font-size: 1.5rem;
  }

  @media (width <= 700px) {
    section {
      margin-bottom: 38px;
    }
  }
</style> -->
